# MySQL Configuration
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/japs?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Japhet_07
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=your-256-bit-secret
jwt.expiration.ms=86400000 # 24 hours
jwt.issuer=inventory-system

# Security Configuration
security.enable-csrf=false
security.cors.allowed-origins=*
security.cors.allowed-methods=GET,POST,PUT,DELETE
security.cors.allowed-headers=*

# Password Encryption
security.password.encoder.strength=10

# Security Note: For production, use environment variables instead of plaintext credentials
